%h1 Movies
 
%table#movies
  %thead
    %tr
      %th Movie
      %th Description
      %th Release Date
      %th Cast
  %tbody
    - @Movies.each do |movie|
      %tr
        %td= movie.title
        %td= movie.description
        %td= movie.release_date.strftime("%m/%d/%Y")
        %td= link_to "Cast and Crew", movie_path(movie)